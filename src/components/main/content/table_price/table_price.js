export const tablePrice = () => {
  const getOffer = document.querySelector(".table-price__get-offer");

  getOffer.onclick = () => {};

  const getOfferMobile = document.querySelector(
    ".table-price__get-offer-mobile"
  );

  getOfferMobile.onclick = () => {};

  const getOfferButtons = document.querySelectorAll(
    ".table-price__get-ofer-buttons"
  );

  getOfferButtons.forEach((buttonItem) => {
    buttonItem.addEventListener("click", () => {});
  });
};
